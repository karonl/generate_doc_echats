export declare function handlePadding(padding?: number[] | number): [number, number, number, number];
/**
 * 多行字符串打印
 * @returns 文案的高度
 */
export declare function fillTexts(ctx: CanvasRenderingContext2D, opt: {
    text?: string;
    width: number;
    lineHeight: number;
}): number;
export declare function getTextHeight(ctx: CanvasRenderingContext2D, opt: {
    text?: string;
    width: number;
    lineHeight: number;
}): number;
/**
 * 通过每个字符串的字节的长度，截取一定长度(width)值的字符串
 * @param str 目标字符串
 * @param width 容器的长度
 * @param byWidth 每个比特所占的长度 默认8
 */
export declare function getSubStr(str: string, width?: number, byWidth?: number): string;
export declare function getRowSpanHeight(trHeights: number[], index: number, rowSpan: number): number;
