import { IColumn } from "./type.js";
export interface BaseConfig {
    /** 单元格的基本宽度 */
    width: number;
    /** 单元格的基本高度 */
    height: number;
    borderColor?: string;
    padding: number[];
    fontSize: string;
    fontFamily: string;
    bgColor: string;
    textAlign: 'left' | 'right' | 'center';
}
export declare class Column {
    width: number;
    textWidth: number;
    height: number;
    column: IColumn;
    baseConfig: BaseConfig;
    parent: Column | null;
    config: IColumn & BaseConfig;
    children?: Column[];
    deep: number;
    colSpan: number;
    constructor(column: IColumn, baseConfig: BaseConfig, parent?: Column | null, deep?: number);
}
export declare function genColumns(columns: IColumn[], baseConfig: BaseConfig, parent?: Column | null, deep?: number): Column[];
/**
 * 获取children或者本身的最后一层数据集合
 * @param columns
 * @returns
 */
export declare function flatColumns(columns: Column[]): Column[];
