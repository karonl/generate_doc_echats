import { Canvas } from 'canvas';
import { Column } from './column.js';
import { TableStyle, TableOpt, TextStyle } from './type.js';
declare class Table2canvas<T extends Record<string, any> = any> {
    canvas: Canvas;
    bgColor: string;
    text?: string;
    /**table title  style */
    textStyle: TextStyle;
    private columns;
    private flatColumns;
    tableWidth: number;
    tableHeight: number;
    headHeight: number;
    dataSource: T[];
    left: number;
    top: number;
    right: number;
    bottom: number;
    /** table style include th and tr's all cell */
    style: Required<TableStyle>;
    /** table padding */
    padding: [number, number, number, number];
    cellPadding: [number, number, number, number];
    width: number | 'auto';
    height: number | 'auto';
    rowHeight: number;
    trHeights: number[];
    ctx: CanvasRenderingContext2D;
    constructor({ canvas, padding, columns, dataSource, style, bgColor, text, textStyle, width, height }: TableOpt<T>);
    private render;
    private renderTh;
    renderThItem(item: Column, x?: number, y?: number): void;
    private renderNoData;
    private renderTr;
    private renderTrItem;
    private renderTitle;
    private initCtxStatus;
    resize(): void;
    private getBodyHeight;
    private getTrHeight;
    appendData(dataSource: T[]): void;
}
export default Table2canvas;
export * from './type.js';
